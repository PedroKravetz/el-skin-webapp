import styled from "styled-components";

const LogoTitle = styled.h1`
  font-family: "Garamond", serif;
  font-size: 2.5rem;
  color: #333;
`;

function Logo() {
  return <LogoTitle>AL SKIN</LogoTitle>;
}

export default Logo;
